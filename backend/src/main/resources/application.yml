server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/worktime_db
    username: worktime
    password: worktime123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  jackson:
    property-naming-strategy: SNAKE_CASE 